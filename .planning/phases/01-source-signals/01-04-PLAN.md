---
phase: 01-source-signals
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - agent/src/chain/clients.ts
  - agent/src/chain/abis.ts
  - agent/src/chain/quote.ts
  - agent/src/chain/reconcile-shared.ts
  - agent/src/chain/recordSignal.ts
  - agent/scripts/registerIdentity.ts
  - agent/test/reconcileShared.test.ts
  - addresses.json
autonomous: false
requirements: [REQ-01, REQ-06]
user_setup:
  - service: erc8004-identity
    why: "Mint the source agent's real ERC-8004 identity on canonical Sepolia IdentityRegistry (D-27)"
    env_vars:
      - name: OPERATOR_PRIVATE_KEY
        source: "Agent operator EOA (from Plan 01) — signs register/record/swap; funded with Sepolia MNT"
      - name: AGENT_URI
        source: "Static GitHub-Pages JSON URL {name,persona,strategy,startedAt,repo} (D-28) — created in Plan 06"

must_haves:
  truths:
    - "A real ERC-8004 agentId is minted via canonical Sepolia IdentityRegistry.register(agentURI) and captured from the mint event — NOT assumed to be 1 (D-27, RESEARCH Pitfall 6)"
    - "The captured agentId is persisted to addresses.json and threaded into registerSource + every recordSignal"
    - "QuoterV2 prices are read via viem simulateContract (non-view) — never readContract (RESEARCH Pattern 2/Pitfall 2)"
    - "The hot path is recordSignal(agentId, 5-field-tuple) → exactInputSingle(8-field router struct) from one EOA; swap revert → invalidateSignal (D-25/D-30)"
    - "The 5-field signal tuple encode/decode lives ONCE in reconcile-shared.ts, imported by both runtime and reconciler (D-40)"
    - "recordSignal.ts (and any chain adapter) reads SOURCE_REGISTRY from addresses.json AT RUNTIME — never a hard-coded throwaway dev address; Plan 06 fills the final redeploy address into addresses.json (W2)"
  artifacts:
    - path: "agent/src/chain/reconcile-shared.ts"
      provides: "Shared 5-field tuple codec (write once, D-40)"
      contains: "encodeAbiParameters"
    - path: "agent/src/chain/recordSignal.ts"
      provides: "recordSignal-then-swap hot path + invalidate-on-revert"
      contains: "exactInputSingle"
    - path: "agent/scripts/registerIdentity.ts"
      provides: "One-shot ERC-8004 mint + registerSource"
    - path: "agent/src/chain/quote.ts"
      provides: "QuoterV2 simulateContract price read"
      contains: "simulateContract"
  key_links:
    - from: "agent/src/chain/recordSignal.ts"
      to: "SourceRegistry.recordSignal then SwapRouter.exactInputSingle"
      via: "2-tx flow from operator EOA, same params, derive 8-field struct from 5-field tuple"
      pattern: "exactInputSingle"
    - from: "agent/scripts/registerIdentity.ts"
      to: "IdentityRegistry.register → registerSource"
      via: "capture returned agentId from mint event, persist, thread through"
      pattern: "register"
    - from: "agent/src/chain/quote.ts"
      to: "QuoterV2.quoteExactInputSingle"
      via: "simulateContract (non-view)"
      pattern: "simulateContract"
---

<objective>
Build the chain layer: mint the real ERC-8004 identity, wire it into registerSource, and implement the
on-chain hot path (the 2-tx recordSignal→swap from one operator EOA) plus the QuoterV2 price read and the
shared signal-tuple codec. This is the bridge between the deployed venue/registry (Plans 01/02) and the
runtime (Plan 05).

Purpose: This plan makes "every trade decision recorded on-chain, performance from on-chain history only"
real (REQ-06). The critical discipline: agentId is captured from the mint (NOT 1 — Pitfall 6); QuoterV2 is
simulated (non-view — Pitfall 2); the 5-field signal tuple is kept distinct from the 8-field router struct
(RESEARCH Pattern 3 anti-pattern); the tuple codec is written once and shared (D-40).

Output: ERC-8004 mint + registerSource one-shot script (with captured agentId persisted); recordSignal.ts
hot path with invalidate-on-revert; quote.ts; reconcile-shared.ts codec + its unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-signals/01-CONTEXT.md
@.planning/phases/01-source-signals/01-RESEARCH.md
@.planning/phases/01-source-signals/01-PATTERNS.md
@.planning/intel/decisions.md
@.planning/intel/constraints.md
@addresses.json
@.planning/phases/01-source-signals/01-01-SUMMARY.md
@.planning/phases/01-source-signals/01-02-SUMMARY.md
@.planning/phases/01-source-signals/01-03-SUMMARY.md

<interfaces>
<!-- Greenfield chain layer. References are RESEARCH Patterns 2/3/5. Use directly. -->

Addresses come from addresses.json (Plan 01 write-back): venue.{factory,swapRouter,quoterV2,
nonfungiblePositionManager}, tokens.{usdc,wmnt,meth,weth}, pools.*, fee=3000, decimals.*.
SourceRegistry address is read AT RUNTIME from addresses.json (`sourceRegistry` field) — Plan 06 writes the
final redeploy address there (W2). The Plan-02 dev deploy address is used ONLY by Plan 02's own DeploySourceRegistryV1
test run and MUST NOT be hard-coded into recordSignal.ts or any adapter; the runtime reads whatever addresses.json holds.
ERC-8004 IdentityRegistry Sepolia: 0x8004A818BFB912233c491871b3d84c89A494BD9e (DEC-004).

ERC-8004 register (RESEARCH Pattern 5, VERIFIED returns next agentId e.g. 137 — NOT 1):
```
register(string agentURI) returns (uint256 agentId)   // capture id from Transfer/Registered event
getAgentWallet(uint256), ownerOf(uint256)             // IIdentityRegistry.sol surface (DEC-005)
```

QuoterV2 (RESEARCH Pattern 2 — NON-VIEW, selector 0xc6a5026a):
```typescript
const { result } = await pub.simulateContract({
  address: QUOTER_V2, abi: quoterV2Abi, functionName: 'quoteExactInputSingle',
  args: [{ tokenIn, tokenOut, amountIn: parseUnits('1', tokenInDecimals), fee: 3000, sqrtPriceLimitX96: 0n }],
}); // returns { amountOut, sqrtPriceX96After, initializedTicksCrossed, gasEstimate }
```

Hot path (RESEARCH Pattern 3) — TWO DISTINCT TUPLES:
- Signal payload (D-07, opaque, 5 fields): abi.encode(tokenIn,tokenOut,amountIn,minAmountOut,fee)
- Router struct (8 fields, selector 0x414bf389 WITH deadline):
  exactInputSingle({tokenIn, tokenOut, fee:3000, recipient:operator, deadline:now+120, amountIn,
  amountOutMinimum:minAmountOut, sqrtPriceLimitX96:0n})
```typescript
const encoded = encodeAbiParameters(
  [{type:'address'},{type:'address'},{type:'uint256'},{type:'uint256'},{type:'uint24'}],
  [tokenIn, tokenOut, amountIn, minAmountOut, fee]);
const recTx = await wallet.writeContract({ address: SOURCE_REGISTRY, abi, functionName:'recordSignal', args:[agentId, encoded] });
const rec = await pub.waitForTransactionReceipt({ hash: recTx });
const signalId = decodeSignalId(rec.logs);   // from SignalRecorded
try { /* exactInputSingle with the 8-field struct */ }
catch (e) { await wallet.writeContract({ ...functionName:'invalidateSignal', args:[agentId, signalId, reason] }); return null; }
```
One-time max approve at startup: `approve(SWAP_ROUTER, type(uint256).max)` per token (D-29).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: viem clients + ABIs + shared 5-field tuple codec (reconcile-shared) + quote</name>
  <read_first>
    - addresses.json (venue/token/pool/fee/decimals)
    - .planning/phases/01-source-signals/01-RESEARCH.md Pattern 2 (quote) + Pattern 3 (tuple encode) + Pattern 4 (reconcile match keys)
    - .planning/phases/01-source-signals/01-PATTERNS.md "reconcile-shared.ts" + "quote.ts" rows + "canonical D-07 5-field signal tuple" shared pattern
    - .planning/phases/01-source-signals/01-02-SUMMARY.md (SourceRegistry event ABI signatures)
  </read_first>
  <behavior>
    - encodeSignal(tokenIn,tokenOut,amountIn,minAmountOut,fee) → 0x bytes; decodeSignal(bytes) → the 5 fields; round-trips exactly
    - the codec matches the Solidity abi.encode of (address,address,uint256,uint256,uint24) — a fixture encoded by forge cast abi-encode decodes identically
    - quote(tokenIn,tokenOut,amountIn) uses simulateContract and returns amountOut as bigint
  </behavior>
  <action>
    Create agent/src/chain/clients.ts: viem publicClient (http RPC https://rpc.sepolia.mantle.xyz) + a
    walletClient from OPERATOR_PRIVATE_KEY (privateKeyToAccount). Create agent/src/chain/abis.ts with the
    minimal ABIs: SourceRegistry (recordSignal, invalidateSignal, signalAt, performance, SignalRecorded,
    SignalDecoded, SignalInvalidated — from the Plan 02 SUMMARY), SwapRouter exactInputSingle (8-field
    struct), QuoterV2 quoteExactInputSingle, IdentityRegistry (register/ownerOf/getAgentWallet), ERC20
    (approve/balanceOf/decimals).
    Create agent/src/chain/reconcile-shared.ts (D-40 — written ONCE): `encodeSignal` /
    `decodeSignal` using viem encodeAbiParameters/decodeAbiParameters over the 5-field tuple; plus a
    `matchKey(tokenIn,tokenOut,amountIn)` helper used by both record + reconcile. Add a header comment:
    "5-FIELD signal tuple — NOT the 8-field router struct (RESEARCH Pattern 3)."
    Create agent/src/chain/quote.ts: `quote(tokenIn,tokenOut,amountIn): Promise<bigint>` via
    `publicClient.simulateContract({...functionName:'quoteExactInputSingle', args:[{tokenIn,tokenOut,
    amountIn,fee:3000,sqrtPriceLimitX96:0n}]})` returning result.amountOut. Persist the raw spot price into
    a price-series buffer the caller owns (for replay determinism, D-02).
    Write agent/test/reconcileShared.test.ts: assert encodeSignal→decodeSignal round-trips; assert a known
    `cast abi-encode "f(address,address,uint256,uint256,uint24)" ...` fixture decodes to identical fields.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/reconcileShared.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/reconcileShared.test.ts` exits 0
    - round-trip test asserts decodeSignal(encodeSignal(x)) deep-equals x for a fixture with fee=3000
    - a test asserts the codec output matches a forge/cast-encoded fixture of (address,address,uint256,uint256,uint24)
    - `grep -n "simulateContract" agent/src/chain/quote.ts` matches; `grep -c "readContract" agent/src/chain/quote.ts` returns 0 (Pitfall 2)
    - `grep -ni "5-FIELD\|NOT the 8-field" agent/src/chain/reconcile-shared.ts` matches (anti-conflation header)
  </acceptance_criteria>
  <done>clients + abis + shared codec + quote built; reconcileShared test green; quote uses simulateContract only.</done>
</task>

<task type="auto">
  <name>Task 2: recordSignal-then-swap hot path + invalidate-on-revert + startup approvals</name>
  <read_first>
    - agent/src/chain/reconcile-shared.ts + abis.ts + clients.ts (Task 1 outputs)
    - .planning/phases/01-source-signals/01-RESEARCH.md Pattern 3 (lines 283-311) + anti-pattern (line 340, two tuples)
    - .planning/phases/01-source-signals/01-CONTEXT.md D-25, D-29, D-30
    - .planning/intel/constraints.md CON-fusionx-router (8-field struct, selector 0x414bf389)
  </read_first>
  <action>
    Create agent/src/chain/recordSignal.ts. Export `ensureApprovals()`: one-time
    `approve(SWAP_ROUTER, 2^256-1)` on all 4 tokens from the operator EOA (D-29), idempotent (skip if
    allowance already max). Export `recordSignalThenSwap(signal): Promise<string | null>`:
      1. Build the 5-field tuple via encodeSignal (reconcile-shared); `recordSignal(agentId, encoded)`;
         waitForTransactionReceipt; decode signalId from the SignalRecorded log.
      2. Derive the 8-field router struct from the 5-field tuple: {tokenIn, tokenOut, fee:3000,
         recipient: operator, deadline: BigInt(Math.floor(Date.now()/1000)+120), amountIn,
         amountOutMinimum: minAmountOut, sqrtPriceLimitX96: 0n}. Call `exactInputSingle`;
         waitForTransactionReceipt.
      3. On swap revert (catch): call `invalidateSignal(agentId, signalId, reason)` (D-30) and return null.
      4. On success: return signalId.
    The amountOutMinimum (from the signal's minAmountOut) is the slippage bound (T-1-12). agentId comes
    from addresses.json (persisted in Task 3). Keep this synchronous/awaited — it is THE hot path; no
    narration here (narration is wired off-path in Plan 05).
  </action>
  <verify>
    <automated>cd agent && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx tsc --noEmit` exits 0 (type-checks)
    - `grep -n "functionName: 'exactInputSingle'" agent/src/chain/recordSignal.ts` matches with all 8 struct fields present (recipient, deadline, sqrtPriceLimitX96 included)
    - `grep -n "functionName: 'invalidateSignal'" agent/src/chain/recordSignal.ts` inside a catch block (D-30 revert handling)
    - `grep -n "amountOutMinimum" agent/src/chain/recordSignal.ts` matches and is set from the signal's minAmountOut (slippage bound)
    - `grep -n "type(uint256).max\|maxUint256\|2n \\*\\* 256n - 1n" agent/src/chain/recordSignal.ts` matches in ensureApprovals (D-29)
    - the function returns null after invalidateSignal on revert (live behavior verified by reconciler in Plan 06)
    - SOURCE_REGISTRY (and venue) addresses are read at runtime from addresses.json — `grep -nE "0x[a-fA-F0-9]{40}" agent/src/chain/recordSignal.ts` returns NO hard-coded address (no throwaway Plan-02 dev address baked in, W2)
  </acceptance_criteria>
  <done>recordSignal.ts hot path type-checks; 2-tx flow with 8-field struct derived from 5-field tuple; invalidate-on-revert; startup max approvals.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Live ERC-8004 mint + registerSource (gated on AGENT_URI from Plan 06)</name>
  <action>Checkpoint: build registerIdentity.ts now (it type-checks); the live mint is deferred until Plan 06 publishes AGENT_URI + redeploys the registry. Steps in how-to-verify.</action>
  <what-built>A one-shot script agent/scripts/registerIdentity.ts that mints the real ERC-8004 identity and wires it into SourceRegistry.registerSource — and persists the captured agentId.</what-built>
  <how-to-verify>
    This task MUST run against the live chain and requires the AGENT_URI (the GitHub-Pages JSON URL, D-28)
    which is created in Plan 06. The script implementation is complete and type-checks now; the live mint
    is gated until AGENT_URI exists.
    Build agent/scripts/registerIdentity.ts (RESEARCH Pattern 5): with OPERATOR_PRIVATE_KEY, call
    IdentityRegistry.register(AGENT_URI); waitForTransactionReceipt; capture the agentId from the
    Transfer(0x0→owner, tokenId) / Registered event (DO NOT assume 1 — Pitfall 6); assert
    ownerOf(agentId) == operator and getAgentWallet(agentId) is set; then call
    SourceRegistry.registerSource(agentId, strategyMeta); persist agentId into addresses.json
    (`agentId` field) + agent/.env. Log the agentId + both tx hashes.
    Run it ONLY after Plan 06 publishes AGENT_URI and redeploys SourceRegistry. The execute-phase orchestrator
    will surface this as the human-action gate (needs the live AGENT_URI + a funded operator EOA).
  </how-to-verify>
  <resume-signal>Type "minted &lt;agentId&gt;" with the captured agentId once register + registerSource land and addresses.json.agentId is set, or "deferred to Plan 06" to proceed (the script is built + type-checked now; the live mint runs in Plan 06's sequence).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator EOA → IdentityRegistry / SourceRegistry / SwapRouter | One key signs mint + record + swap; key in gitignored .env |
| thin mock pools → swap | Front-run/sandwich risk bounded by amountOutMinimum |
| mint event → agentId | The id is read from chain, never hard-coded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing/Elevation | OPERATOR_PRIVATE_KEY leak | mitigate | viem privateKeyToAccount reads from gitignored .env; .env.example names only; testnet-only funds (D-41); rotate on exposure |
| T-1-12 | Tampering | swap front-run / sandwich on thin mock pools | mitigate | amountOutMinimum (from the 5-field tuple) is the slippage bound on exactInputSingle; full-range LP + ambient bot keep depth (Plan 01/05); revert → invalidateSignal (D-30) |
| T-1-13 | Tampering | wrong agentId (Phase-0 placeholder 1) attaches signals to the wrong identity / reverts NotSourceOwner | mitigate | agentId captured from the mint event at register time, persisted to addresses.json, threaded through registerSource + recordSignal (Pitfall 6); script asserts ownerOf(agentId)==operator |
| T-1-06 | DoS | swap/quote sent to a dead venue address | mitigate | All venue addresses read from addresses.json (codesize-verified in Plan 01), never from stale FusionX consts (D-43) |
</threat_model>

<verification>
- `cd agent && npx tsc --noEmit` clean
- `npx vitest run test/reconcileShared.test.ts` green
- quote.ts uses simulateContract only (no readContract)
- recordSignal.ts: 8-field struct, invalidate-on-revert, amountOutMinimum bound, max approvals
- registerIdentity captures agentId from event (not 1)
</verification>

<success_criteria>
- Shared 5-field tuple codec written once (D-40), round-trip + cast-fixture tested
- QuoterV2 read via simulateContract (Pitfall 2)
- recordSignal→swap hot path with invalidate-on-revert + startup max approvals (D-25/D-29/D-30)
- ERC-8004 mint + registerSource script captures the real agentId (Pitfall 6); live mint gated on Plan 06 AGENT_URI
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-signals/01-04-SUMMARY.md` documenting the chain-layer
exports Plan 05 imports (recordSignalThenSwap, quote, ensureApprovals, encodeSignal/decodeSignal), the
agentId-capture mechanism, and the deferred live-mint sequencing into Plan 06.
</output>
