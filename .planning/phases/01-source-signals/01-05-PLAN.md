---
phase: 01-source-signals
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - agent/src/store/thesisStore.ts
  - agent/src/health.ts
  - agent/src/config.ts
  - agent/src/index.ts
  - agent/scripts/reconcile.ts
  - agent/scripts/noiseBot.ts
  - agent/test/reconcile.test.ts
autonomous: true
requirements: [REQ-01, REQ-06]

must_haves:
  truths:
    - "The 30s poll loop order is: deterministic decision → await recordSignalThenSwap (HOT PATH) → void narrateAndStore (NEVER awaited) (AI-SPEC §4, D-04)"
    - "Narration is fire-and-store: a slow/failed Claude call can never delay or block the next tick or the trade (RESEARCH Pitfall 5)"
    - "Per-pair cooldown ~3-5 min + ~20 signals/day soft cap enforced agent-side (D-06/D-10); agent has its own pause flag (D-11)"
    - "scripts/reconcile.ts walks SignalRecorded + SignalInvalidated, matches each non-invalidated signal to a settled swap, reports {matched,invalidated,orphan}, exits non-zero on any orphan (D-40 acceptance gate)"
    - "The reconciler imports reconcile-shared.ts — the same codec the runtime uses, not reimplemented (D-40)"
    - "Thesis JSON written to theses/<agentId>/<signalId>.json off-chain (D-09/D-37); /healthz returns lastTickAt/lastSignalAt (D-38)"
  artifacts:
    - path: "agent/src/index.ts"
      provides: "30s poll loop controller"
      contains: "void narrateAndStore"
    - path: "agent/scripts/reconcile.ts"
      provides: "Reconciliation CLI + --assert acceptance gate (D-40)"
      contains: "orphan"
    - path: "agent/src/health.ts"
      provides: "/healthz endpoint (D-38)"
    - path: "agent/scripts/noiseBot.ts"
      provides: "Ambient noise bot, separate EOA (D-24)"
  key_links:
    - from: "agent/src/index.ts"
      to: "recordSignalThenSwap (await) + narrateAndStore (void)"
      via: "hot path awaited, narration fire-and-store"
      pattern: "void narrateAndStore"
    - from: "agent/scripts/reconcile.ts"
      to: "agent/src/chain/reconcile-shared.ts"
      via: "shared codec import (D-40)"
      pattern: "reconcile-shared"
    - from: "agent/src/store/thesisStore.ts"
      to: "theses/<agentId>/<signalId>.json + git push"
      via: "off-chain write via PAT (D-37)"
      pattern: "theses"
---

<objective>
Assemble the full agent runtime: the 30s poll loop that ties the pure strategy (Plan 03), the chain hot
path (Plan 04), and the off-path narration together — plus the off-chain thesis store, the /healthz
endpoint, the ambient-noise bot, and the reconciler CLI that is BOTH a deliverable and the Phase 1
acceptance gate (D-40).

Purpose: This is where the pieces become a running agent. The single most important invariant: the loop
awaits the on-chain trade then fires narration without awaiting it (RESEARCH Pitfall 5 / AI-SPEC §4) — a
slow Claude can never stall the loop or the trade. The reconciler proves "performance from on-chain
history only" (REQ-06) and gates the phase: orphan == 0 over all non-invalidated signals (D-40).

Output: index.ts poll loop; thesisStore; health; config; reconcile.ts (--assert mode) + its unit test;
noiseBot.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-signals/01-CONTEXT.md
@.planning/phases/01-source-signals/01-AI-SPEC.md
@.planning/phases/01-source-signals/01-RESEARCH.md
@.planning/phases/01-source-signals/01-PATTERNS.md
@addresses.json
@.planning/phases/01-source-signals/01-03-SUMMARY.md
@.planning/phases/01-source-signals/01-04-SUMMARY.md

<interfaces>
<!-- Imports from Plans 03 + 04. Use the documented exports directly. -->

From Plan 03 (agent/src/strategy + narration):
- decideSignals(priceSeriesByPair, portfolio, config): Signal[]   // pure core
- narrateSignalSafe(signal, signalId): Promise<string>            // never throws

From Plan 04 (agent/src/chain):
- ensureApprovals(): Promise<void>                                // startup max approvals (D-29)
- recordSignalThenSwap(signal): Promise<string | null>           // HOT PATH; null on revert (already invalidated)
- quote(tokenIn, tokenOut, amountIn): Promise<bigint>            // QuoterV2 simulate
- encodeSignal/decodeSignal/matchKey                              // shared codec (D-40)
- SourceRegistry + SwapRouter ABIs (abis.ts); publicClient/walletClient (clients.ts)

Poll-loop ordering (AI-SPEC §4 handleSignal, lines 288-306):
```typescript
async function handleSignal(signal: Signal) {
  const signalId = await recordSignalThenSwap(signal);  // HOT PATH — await
  if (signalId === null) return;                          // reverted → invalidateSignal already emitted
  void narrateAndStore(signal, signalId);                 // OFF HOT PATH — never awaited
}
async function narrateAndStore(signal, signalId) {
  const thesis = await narrateSignalSafe(signal, signalId);  // never throws
  await writeThesis(signal.agentId, signalId, thesis);
}
```

Reconciler (RESEARCH Pattern 4, D-40):
```typescript
const signals = await pub.getLogs({ address: SOURCE_REGISTRY, event: signalRecordedAbi, args:{ agentId }, fromBlock, toBlock });
const invalidated = await pub.getLogs({ address: SOURCE_REGISTRY, event: signalInvalidatedAbi, args:{ agentId }, fromBlock, toBlock });
// match each non-invalidated signal to a settled swap by (operator, tokenIn, tokenOut, amountIn) via matchKey
// report { matched, invalidated, orphan }; --assert exits non-zero if orphan > 0
```
Strategy config: per-pair cooldown 3-5 min (D-06), ~20 signals/day soft cap (D-10), pair order WMNT,mETH,WETH (D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config + thesisStore + health + poll loop (index.ts)</name>
  <read_first>
    - .planning/phases/01-source-signals/01-AI-SPEC.md §4 (handleSignal ordering) + §3 project structure
    - .planning/phases/01-source-signals/01-04-SUMMARY.md (chain-layer exports) + 01-03-SUMMARY.md (strategy/narration exports)
    - .planning/phases/01-source-signals/01-CONTEXT.md D-04 (30s poll), D-06 (cooldown), D-09/D-37 (thesis store), D-10 (daily cap), D-11 (pause flag), D-38 (healthz)
    - .planning/phases/01-source-signals/01-RESEARCH.md Pitfall 5 (narration must never block/crash the loop)
  </read_first>
  <action>
    Create agent/src/config.ts: load addresses.json + env (OPERATOR_PRIVATE_KEY, ANTHROPIC_API_KEY,
    GITHUB_PAT, agentId), strategy constants (pollMs=30_000, shortWindow=5, longWindow=20, cooldownMs=240_000,
    dailySoftCap=20, buyFraction=0.30, pairOrder=['WMNT/USDC','mETH/USDC','WETH/USDC']).
    FAIL-CLOSED GUARD (W3): config.ts MUST THROW at module load / boot if any REQUIRED value is unset, empty,
    or the zero value — specifically agentId (must be a positive integer, NOT 0/undefined/placeholder),
    sourceRegistry, swapRouter, quoterV2, and each of the 4 token addresses (each a non-zero 0x-address), and
    OPERATOR_PRIVATE_KEY. Export an `assertConfig()` that validates all of these and invoke it at the top of
    index.ts, reconcile.ts, and noiseBot.ts before any chain call. This prevents a misconfigured boot from
    calling recordSignal with agentId `undefined`/`0` and silently reverting `NotSourceOwner` (Pitfall 6, T-1-13).
    Create agent/src/store/thesisStore.ts: `writeThesis(agentId, signalId, thesis)` writes
    theses/<agentId>/<signalId>.json `{agentId, signalId, thesis, ts}` then git-commits + pushes via the
    GITHUB_PAT (D-37); use rolling/daily-rotated subfolders to avoid commit-flood (Claude's discretion on
    scheme). Never read a thesis back into a prompt (AI-SPEC §4 stateless).
    Create agent/src/health.ts: a tiny HTTP server exposing GET /healthz → `{lastTickAt, lastSignalAt,
    fallbackRate, paused}` (D-38), reading from a shared status object the loop updates each tick.
    Create agent/src/index.ts: the 30s poll loop. Each tick: skip if paused (D-11); for each pair in fixed
    order (D-16) quote the price (Plan 04 quote), push to the per-pair price-series buffer, run decideSignals
    (Plan 03) against the live portfolio + per-pair cooldown (D-06) + daily soft cap (D-10); for each emitted
    signal call handleSignal — await recordSignalThenSwap, then `void narrateAndStore(signal, signalId)`
    (NEVER awaited). On startup call ensureApprovals once. Update health status each tick (lastTickAt) and
    each signal (lastSignalAt). Emit one structured stdout log line per narration
    {signalId, pair, direction, model, durationMs, retried, fallbackUsed, reason?} (AI-SPEC §7).
    Attach a .catch on the void narrateAndStore as a belt-and-suspenders against unhandledRejection (it
    already never throws via narrateSignalSafe).
  </action>
  <verify>
    <automated>cd agent && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx tsc --noEmit` exits 0
    - `grep -n "void narrateAndStore" agent/src/index.ts` matches and is NOT preceded by `await` on that line (fire-and-store, Pitfall 5)
    - `grep -n "await recordSignalThenSwap" agent/src/index.ts` matches (hot path awaited)
    - `grep -n "/healthz" agent/src/health.ts` matches; response includes lastTickAt + lastSignalAt
    - `grep -n "theses" agent/src/store/thesisStore.ts` matches the path pattern theses/<agentId>/<signalId>.json
    - `grep -n "cooldownMs\|dailySoftCap\|pairOrder" agent/src/config.ts` matches (D-06/D-10/D-16)
    - config.ts throws at boot on a missing/zero required value: `grep -nE "throw |assertConfig" agent/src/config.ts` matches; the guard rejects agentId of 0/undefined and any zero/empty venue/registry/token address (W3, fail-closed — no silent `NotSourceOwner` revert)
  </acceptance_criteria>
  <done>Poll loop type-checks; narration is fire-and-store (never awaited); /healthz + thesisStore + config present; cooldown/cap/pair-order wired.</done>
</task>

<task type="auto">
  <name>Task 2: reconciler CLI (--assert acceptance gate) + unit test</name>
  <read_first>
    - agent/src/chain/reconcile-shared.ts (the shared codec to import — D-40, do not reimplement)
    - .planning/phases/01-source-signals/01-RESEARCH.md Pattern 4 (lines 313-323)
    - .planning/phases/01-source-signals/01-CONTEXT.md D-40 (acceptance gate) + D-34 (best-effort ordering)
    - .planning/phases/01-source-signals/01-VALIDATION.md (reconciler rows: orphan==0, exit non-zero)
    - .planning/phases/01-source-signals/01-02-SUMMARY.md (SignalRecorded/SignalInvalidated event ABIs)
  </read_first>
  <action>
    Create agent/scripts/reconcile.ts (D-40). Import the shared codec from agent/src/chain/reconcile-shared.ts
    (NOT a reimplementation). Steps: getLogs(SignalRecorded) + getLogs(SignalInvalidated) for the agentId over
    the registry's block range; decode each signal's 5-field tuple via decodeSignal; pull the operator EOA's
    settled swaps over the same block range (filter exactInputSingle txs to SWAP_ROUTER from operator, OR the
    pool Swap events with recipient=operator); match each NON-invalidated signal to a swap by
    matchKey(tokenIn,tokenOut,amountIn) (D-34 best-effort — recordSignal block ≤ swap block from same EOA);
    classify {matched, invalidated, orphan}. Default mode prints the JSON report + a per-signal table.
    `--assert` mode: exit code 1 if any NON-invalidated signal is orphan, else exit 0 (the D-40 gate).
    Also report an ordering check: for each matched signal assert recordSignal tx block ≤ swap tx block.
    Add package.json script `reconcile` → `tsx scripts/reconcile.ts`.
    Write agent/test/reconcile.test.ts: with mocked getLogs returning a fixed set of (signals, invalidated,
    swaps), assert the classifier returns the correct {matched, invalidated, orphan} counts — including a case
    where an invalidated signal with no matching swap counts as `invalidated` not `orphan` (D-30), and a case
    where a non-invalidated signal with no swap is an `orphan` (would fail --assert). Keep the classifier a
    pure function so it is unit-testable without a live chain.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/reconcile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/reconcile.test.ts` exits 0
    - `grep -n "reconcile-shared" agent/scripts/reconcile.ts` matches (shared codec imported, D-40 — not reimplemented)
    - `grep -n "orphan" agent/scripts/reconcile.ts` matches in both the report and the --assert exit logic
    - test asserts: invalidated-without-swap → counted `invalidated` (not orphan); non-invalidated-without-swap → `orphan`
    - test asserts the classifier is a pure function (called with fixture arrays, no network)
    - `grep -n "process.exit(1)\|exitCode = 1" agent/scripts/reconcile.ts` matches under the --assert orphan>0 branch
  </acceptance_criteria>
  <done>Reconciler CLI built importing the shared codec; --assert exits non-zero on orphan; classifier unit-tested for matched/invalidated/orphan including the D-30 invalidated case.</done>
</task>

<task type="auto">
  <name>Task 3: ambient noise bot</name>
  <read_first>
    - agent/src/chain/recordSignal.ts (exactInputSingle call pattern to reuse for plain swaps)
    - agent/src/chain/clients.ts (wallet client construction — but use NOISE_BOT_PRIVATE_KEY)
    - .planning/phases/01-source-signals/01-CONTEXT.md D-24 (ambient noise bot spec)
    - .planning/phases/01-source-signals/01-RESEARCH.md Pattern 3 (swap call)
  </read_first>
  <action>
    Create agent/scripts/noiseBot.ts (D-24): a SEPARATE process using NOISE_BOT_PRIVATE_KEY (its own EOA,
    NOT the operator — keeps it out of the agent's track record). Every 1-3 min (randomized), pick a random
    pair and a random small amount (~0.5-2% of that pool's depth) and do a plain exactInputSingle swap to
    nudge the price so the momentum MAs cross organically. It does NOT call recordSignal — it is pure market
    utility, not a signal source. Mint itself tokens via the public MockERC20 mint at startup if balance is
    low. Add package.json script `noise` → `tsx scripts/noiseBot.ts`. Structured stdout per swap.
  </action>
  <verify>
    <automated>cd agent && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx tsc --noEmit` exits 0
    - `grep -n "NOISE_BOT_PRIVATE_KEY" agent/scripts/noiseBot.ts` matches (separate EOA, D-24)
    - `grep -c "recordSignal" agent/scripts/noiseBot.ts` returns 0 (noise bot never records signals — not part of the track record)
    - `grep -n "exactInputSingle" agent/scripts/noiseBot.ts` matches (does real swaps)
    - randomization present: `grep -n "Math.random" agent/scripts/noiseBot.ts` matches
  </acceptance_criteria>
  <done>Noise bot type-checks; uses a separate EOA; does real swaps; never records signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| narration (untrusted, slow) → poll loop | Narration runs off the hot path; can never block/crash the loop |
| GITHUB_PAT → thesis repo push | Fine-scoped PAT writes thesis JSON only |
| noise bot EOA → pools | Separate key; not part of the track record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | DoS | un-awaited narration rejection crashes the long-running VPS process | mitigate | narrateSignalSafe never throws (Plan 03) + a belt-and-suspenders .catch on void narrateAndStore; /healthz lastTickAt detects a hung loop (D-38, Pitfall 5) |
| T-1-14 | Elevation/Information Disclosure | GitHub PAT leak (thesis push) | mitigate | Fine-scoped PAT (single repo, contents-write only); gitignored .env; rotate on exposure |
| T-1-15 | DoS | runaway signal flood drains pools / spams chain | mitigate | Per-pair cooldown (D-06) + ~20/day soft cap (D-10) + agent pause flag (D-11) enforced in the loop |
| T-1-07 | Repudiation/optics | bad thesis published to the card | mitigate | narrateSignalSafe + the Plan 03 fidelity/banned-phrase gates run pre-publish; fallback substitution on any trip (AI-SPEC §6) |
</threat_model>

<verification>
- `cd agent && npx tsc --noEmit` clean across the workspace
- `npx vitest run` green (reconcile.test.ts + all prior)
- Poll loop: hot path awaited, narration fire-and-store (grep gates)
- Reconciler imports the shared codec; --assert exits non-zero on orphan
- Noise bot uses separate EOA, never records signals
</verification>

<success_criteria>
- 30s poll loop: decision → await trade → void narration (never blocks the loop, Pitfall 5)
- Cooldown + daily cap + pause flag enforced (D-06/D-10/D-11)
- Reconciler CLI with --assert acceptance gate (orphan==0), shared codec (D-40), unit-tested classifier
- Thesis store + /healthz + ambient noise bot built
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-signals/01-05-SUMMARY.md` documenting the runtime
entry points (npm run agent / reconcile / noise), the /healthz contract, and the exact `reconcile.ts
--assert` command that is the Phase 1 acceptance gate.
</output>
