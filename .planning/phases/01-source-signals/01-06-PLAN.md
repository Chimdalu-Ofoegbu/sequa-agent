---
phase: 01-source-signals
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - deployments/sepolia.json
  - .planning/phases/00-lock/DEPLOYMENT.md
  - RUN.md
  - agentURI.json
  - agent/Dockerfile
  - agent/.env.example
  - addresses.json
autonomous: false
requirements: [REQ-01, REQ-06]
user_setup:
  - service: erc8004-identity
    why: "Live mint of the source agent's ERC-8004 identity once AGENT_URI is published (D-27)"
    env_vars:
      - name: OPERATOR_PRIVATE_KEY
        source: "Agent operator EOA (Plan 01); funded with Sepolia MNT"
      - name: AGENT_URI
        source: "Published GitHub-Pages JSON URL (this plan creates agentURI.json + publishes it)"
  - service: vps-host
    why: "Always-on host so a multi-day track record accrues before the Phase 5 demo (D-35/D-36)"
    dashboard_config:
      - task: "Deploy the agent container; set .env (OPERATOR_PRIVATE_KEY, ANTHROPIC_API_KEY, GITHUB_PAT, agentId); expose /healthz"
        location: "Railway / Fly / cheap VPS (Claude's discretion per D-35)"
      - task: "Create an UptimeRobot monitor on /healthz with a Discord/Slack alert (D-38)"
        location: "UptimeRobot free tier"
  - service: github-pages-and-pat
    why: "Host agentURI.json (D-28) + push thesis JSON (D-37)"
    env_vars:
      - name: GITHUB_PAT
        source: "Fine-scoped PAT (single repo, contents-write only) — GitHub → Settings → Developer settings → Fine-grained tokens"

must_haves:
  truths:
    - "The extended SourceRegistry is redeployed + verified on Mantle Explorer; deployments/sepolia.json + DEPLOYMENT.md cite the Phase 1 address (D-31)"
    - "The agent's ERC-8004 identity is minted live; ownerOf(agentId)==operator; agentId is wired into registerSource + the runtime (D-27, Pitfall 6)"
    - "At least one live signal completes the recordSignal→swap path end-to-end on the seeded venue (REQ-06)"
    - "reconcile.ts --assert reports {matched:N, invalidated:M, orphan:0} and exits 0 — the Phase 1 acceptance gate (D-40)"
    - "The agent runs on an always-on host with /healthz + UptimeRobot; RUN.md documents the <3-min local-run failover (D-35/D-38/D-39/D-42)"
  artifacts:
    - path: "deployments/sepolia.json"
      provides: "Updated manifest: Phase 1 SourceRegistry + mocks + venue + pools"
      contains: "sourceRegistry"
    - path: "RUN.md"
      provides: "Reproducibility + local-run failover story (D-39/D-42)"
      contains: "npm run agent"
    - path: "agentURI.json"
      provides: "Static agent identity JSON for ERC-8004 agentURI (D-28)"
      contains: "persona"
  key_links:
    - from: "agent/scripts/registerIdentity.ts"
      to: "live IdentityRegistry.register(AGENT_URI) → registerSource"
      via: "captured agentId persisted to addresses.json"
      pattern: "register"
    - from: "reconcile.ts --assert"
      to: "live on-chain signal history"
      via: "orphan == 0 over all non-invalidated signals"
      pattern: "orphan"
---

<objective>
Take Phase 1 live and close the acceptance gate. Redeploy the extended SourceRegistry on Sepolia + verify;
publish AGENT_URI and mint the real ERC-8004 identity (wiring the captured agentId everywhere); run the
first live recordSignal→swap; host the agent always-on with /healthz + UptimeRobot; write RUN.md; update
DEPLOYMENT.md; and prove the D-40 gate: `reconcile.ts --assert` reports orphan == 0.

Purpose: This is the phase's proof. Every prior plan produced tested code; this plan executes it against
the live seeded venue and demonstrates the four Phase 1 success criteria — a Claude-driven agent recording
every decision on-chain, performance from on-chain history only, executions landing on the venue, and
performance(agentId) readable. Boot the agent the moment smoke passes (D-36) so a 2-5 day track record
accrues before the Phase 5 demo.

Output: redeployed + verified registry; minted agentId; first live signal; reconciler gate passing; hosted
agent + uptime monitor; RUN.md + updated DEPLOYMENT.md + manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-signals/01-CONTEXT.md
@.planning/intel/decisions.md
@addresses.json
@deployments/sepolia.json
@.planning/phases/00-lock/DEPLOYMENT.md
@.planning/phases/01-source-signals/01-01-SUMMARY.md
@.planning/phases/01-source-signals/01-02-SUMMARY.md
@.planning/phases/01-source-signals/01-04-SUMMARY.md
@.planning/phases/01-source-signals/01-05-SUMMARY.md

<interfaces>
<!-- Live execution against artifacts from prior plans. -->

Redeploy: `forge script script/DeploySourceRegistryV1.s.sol:DeploySourceRegistryV1 --rpc-url mantle_sepolia --broadcast --slow`
Verify: `set -a; source .env; set +a; bash script/VerifyPhase1.sh`  (Etherscan V2, chainid=5003)
Mint + register: `cd agent && npx tsx scripts/registerIdentity.ts`   (captures agentId from mint event — NOT 1, Pitfall 6)
Acceptance gate: `cd agent && npx tsx scripts/reconcile.ts --assert`  (exit 0 iff orphan == 0, D-40)
Run agent: `cd agent && npm run agent`   ;  Noise bot: `npm run noise`   ;  Health: GET /healthz → {lastTickAt,lastSignalAt}

agentURI.json shape (D-28): { name, persona, strategy, startedAt, repo }
ERC-8004 IdentityRegistry Sepolia: 0x8004A818BFB912233c491871b3d84c89A494BD9e (DEC-004)

Persona copy (planner-drafted in Claude's voice, D-12, within the momentum-trader constraint D-08):
- name: "MOMENTUM"
- persona: "A confident momentum trader — patient until the trend confirms, then in fast and out faster on the reversal."
- strategy: "Deterministic 5/20 moving-average crossover on WMNT/USDC, mETH/USDC, WETH/USDC; every decision recorded on-chain."
(Executor may refine copy within D-08/D-12; do not change the strategy facts.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish AGENT_URI + redeploy/verify SourceRegistry + update manifest & DEPLOYMENT.md</name>
  <read_first>
    - script/DeploySourceRegistryV1.s.sol (Plan 02) + script/VerifyPhase1.sh (Plan 01)
    - deployments/sepolia.json (manifest shape) + .planning/phases/00-lock/DEPLOYMENT.md (table + REQ-12 checklist to update)
    - .planning/phases/01-source-signals/01-CONTEXT.md D-12 (persona), D-28 (agentURI), D-31 (redeploy + DEPLOYMENT.md update)
    - .planning/phases/01-source-signals/01-02-SUMMARY.md (new registry ABI)
  </read_first>
  <action>
    Create agentURI.json at repo root with the D-12/D-28 persona JSON (interfaces block). Publish it to a
    resolvable HTTPS URL (GitHub Pages or raw.githubusercontent.com — Claude's discretion); set AGENT_URI in
    agent/.env to that URL. (RESEARCH Open Q2: the registry stores an arbitrary string; a resolvable URL is
    the portable choice for the Phase 4 card.)
    Redeploy the extended SourceRegistry:
    `forge script script/DeploySourceRegistryV1.s.sol:DeploySourceRegistryV1 --rpc-url mantle_sepolia --broadcast --slow`.
    Capture the new address + deploy tx. Verify it: `bash script/VerifyPhase1.sh` (covers the registry + the
    4 mocks). Update deployments/sepolia.json: set contracts.sourceRegistry to the NEW Phase 1 address +
    deployTx + explorerUrl + verificationUrl + verified=true; add the 4 mocks, the UniV3 venue, and the 3
    pools. Update addresses.json with the new sourceRegistry address. Update
    .planning/phases/00-lock/DEPLOYMENT.md: replace the SourceRegistry row with the Phase 1 redeploy address
    (note "Phase 5 cites THIS address" per D-31), add mock-token rows, note the venue is a self-deployed
    UniV3 fork (D-43). Do NOT touch FollowRegistry (D-31).
  </action>
  <verify>
    <automated>bash -c 'set -a; source .env 2>/dev/null; set +a; A=$(jq -r ".contracts.sourceRegistry.address" deployments/sepolia.json); cs=$(cast codesize "$A" --rpc-url https://rpc.sepolia.mantle.xyz); echo "registry $A codesize=$cs"; [ "$cs" -gt 0 ] && cast call "$A" "performance(uint256)(uint256,uint64)" 1 --rpc-url https://rpc.sepolia.mantle.xyz'</automated>
  </verify>
  <acceptance_criteria>
    - new SourceRegistry address in deployments/sepolia.json has codesize > 0 on chain 5003 and differs from the Phase 0 address 0x97a724ca8d70aee206b8d56925a735511d3cd5c8 (fresh redeploy, RESEARCH State Inventory)
    - `cast call <registry> "signalAt(uint256,uint256)(bytes)" ...` is callable (the new ABI is live) — proves the extended contract was deployed, not the skeleton
    - `jq -e '.tokens and .venue and .pools' addresses.json` exits 0 and `.contracts.sourceRegistry.verified == true` in deployments/sepolia.json
    - `grep -ni "Phase 1" .planning/phases/00-lock/DEPLOYMENT.md` shows the SourceRegistry row updated to the redeploy address
    - `jq -e '.name and .persona and .strategy' agentURI.json` exits 0; AGENT_URI resolves over HTTPS (curl -sf returns the JSON)
  </acceptance_criteria>
  <done>AGENT_URI published; extended SourceRegistry redeployed + verified at a new address; manifest + addresses.json + DEPLOYMENT.md updated; FollowRegistry untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Live ERC-8004 mint + registerSource</name>
  <action>Checkpoint: run registerIdentity.ts against the live chain; capture the real agentId from the mint event (NOT 1) and confirm registerSource landed. Steps in how-to-verify.</action>
  <what-built>The mint script (Plan 04 registerIdentity.ts) is ready and AGENT_URI is now published + the registry redeployed. This step performs the live ERC-8004 mint + registerSource, which writes on-chain from the operator EOA and cannot be done by tooling without the funded key + live URL.</what-built>
  <how-to-verify>
    Run: `cd agent && npx tsx scripts/registerIdentity.ts`.
    (Pre-verified non-destructively in 01-SPIKE-FINDINGS.md § Spike #3: `register(string)` is OPEN — no fee, no
    allowlist — and costs ~181k gas; the next agentId was 146 at probe time. This mint is therefore low-risk.)
    Confirm in its output: a real agentId was captured from the mint event (it will NOT be 1 — expect ~146+
    per RESEARCH Pitfall 6 / SPIKE-FINDINGS); `ownerOf(agentId) == operator` and `getAgentWallet(agentId)` is set; and
    registerSource(agentId, strategyMeta) landed. Confirm addresses.json now has the `agentId` field set and
    agent/.env has agentId. Open the register tx + registerSource tx on https://sepolia.mantlescan.xyz to
    confirm both succeeded.
  </how-to-verify>
  <resume-signal>Type "minted &lt;agentId&gt;" with the captured agentId once both txs succeed and addresses.json.agentId is set.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Host always-on, first live signal, /healthz + UptimeRobot, RUN.md</name>
  <action>Checkpoint: deploy + host the agent, wait for the first live recordSignal→swap, run reconcile.ts --assert (orphan==0), set up UptimeRobot, write RUN.md, leave the agent running. Steps in how-to-verify.</action>
  <what-built>Hosted the agent always-on (Dockerfile + host deploy), booted the poll loop + noise bot, ran the first live signal, set up UptimeRobot on /healthz, and wrote RUN.md. (The D-40 orphan==0 acceptance gate is a SEPARATE blocking checkpoint — Task 4.)</what-built>
  <how-to-verify>
    1. Create agent/Dockerfile (Node 20 base, install + tsx) and deploy the agent to the chosen host
       (Railway/Fly/VPS — D-35) with .env set (OPERATOR_PRIVATE_KEY, ANTHROPIC_API_KEY, GITHUB_PAT, agentId);
       start `npm run agent` + `npm run noise`. Confirm `curl https://<host>/healthz` returns fresh
       lastTickAt (≤ 2 min) + lastSignalAt (D-38).
    2. Wait for at least one live signal (the noise bot keeps the MAs crossing). Confirm a SignalRecorded
       event appears on the new SourceRegistry on the explorer and a matching swap tx from the operator EOA
       on the SwapRouter — the recordSignal→swap path end-to-end (REQ-06).
    3. Confirm a thesis JSON appeared at theses/<agentId>/<signalId>.json in the public repo (D-37).
    4. Create an UptimeRobot monitor on /healthz with a Discord/Slack alert (D-38); force a brief outage and
       confirm the alert fires.
    5. Write RUN.md (D-42): strategy rule, Claude prompts, reconciler usage, /healthz, and the <3-min
       local-run failover ("pull repo + .env + npm run agent" against the same operator EOA / same
       SourceRegistry / same agentId — D-39). Do a local-run drill on a second machine to confirm <3 min.
    6. Leave the agent running (D-36) so a 2-5 day track record accrues before the Phase 5 demo.
    NOTE (W4): the D-40 `reconcile.ts --assert` orphan==0 acceptance gate is its OWN dedicated blocking
    checkpoint (Task 4 below) — a hosting "approve" here MUST NOT mask a reconciliation failure.
  </how-to-verify>
  <resume-signal>Type "approved" once /healthz is live + green on UptimeRobot, at least one recordSignal→swap pair is visible on the explorer, RUN.md documents the local-run failover, and the agent is left running — or describe what failed. (The orphan==0 gate is verified separately in Task 4.)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: D-40 ACCEPTANCE GATE — reconcile.ts --assert reports orphan == 0</name>
  <action>Checkpoint (W4 — the dedicated phase acceptance gate, split out of Task 3 so a hosting "approve" cannot mask a reconciliation failure): run the reconciler in --assert mode over the full live signal history and confirm orphan == 0. Steps in how-to-verify.</action>
  <what-built>The reconciler CLI (Plan 05 scripts/reconcile.ts) is built + unit-tested; the agent has been live (Task 3) and has accrued at least one recordSignal→swap pair. This checkpoint runs the D-40 acceptance gate against the live on-chain signal history.</what-built>
  <how-to-verify>
    1. Run the ACCEPTANCE GATE: `cd agent && npx tsx scripts/reconcile.ts --assert`.
    2. Confirm it prints `{matched: N, invalidated: M, orphan: 0}` and exits 0 (D-40). Every non-invalidated
       signal must match a settled swap; every reverted swap must be counted `invalidated` (not `orphan`, D-30).
    3. If orphan > 0, the GATE FAILS — do NOT approve. Investigate the orphan signal(s) (missing swap, wrong
       match key, or an un-invalidated revert) and re-run until orphan == 0.
  </how-to-verify>
  <resume-signal>Type "gate passed: matched=&lt;N&gt; invalidated=&lt;M&gt; orphan=0" once `reconcile.ts --assert` exits 0 with orphan == 0, or describe the orphan(s) that failed the gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator EOA → live mint + record + swap | One key signs all on-chain writes; lives on the host in gitignored .env |
| public internet → /healthz | Read-only liveness endpoint; no secrets exposed |
| host → secrets (.env, PAT) | Secrets live on the host, never in git (RUN.md documents, never commits them) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing/Elevation | OPERATOR_PRIVATE_KEY on the host | mitigate | Host .env gitignored; RUN.md documents contents by NAME only; testnet-only funds (D-41); separate keys per role (deployer/operator/noise, D-24/D-26) |
| T-1-14 | Elevation | GITHUB_PAT on the host (thesis push) | mitigate | Fine-scoped PAT (single repo, contents-write only); gitignored; rotate on exposure |
| T-1-16 | DoS | silent VPS death (agent stops, track record stalls before demo) | mitigate | /healthz lastTickAt + external UptimeRobot ping with Discord/Slack alert on staleness (D-38); documented <3-min local-run failover (D-39) |
| T-1-06 | DoS | redeploy/mint sent to a wrong/dead address | mitigate | All addresses read from addresses.json / deployments/sepolia.json (codesize-verified, Plan 01); registry codesize asserted post-redeploy; never the dead FusionX consts (D-43) |
| T-1-13 | Tampering | wrong agentId wired into the live runtime | mitigate | agentId captured from the live mint event, persisted, asserted ownerOf==operator before registerSource (Pitfall 6) |
</threat_model>

<verification>
- Redeployed SourceRegistry codesize > 0 and exposes the new ABI (signalAt callable)
- agentId minted live and captured (not 1); ownerOf(agentId)==operator
- At least one recordSignal→swap pair visible on the explorer
- `reconcile.ts --assert` → orphan == 0, exit 0 (D-40 acceptance gate)
- /healthz live + UptimeRobot green; RUN.md documents <3-min local failover
</verification>

<success_criteria>
- Extended SourceRegistry redeployed + verified at the Phase 1 address; DEPLOYMENT.md/manifest updated (D-31)
- Real ERC-8004 identity minted; agentId wired through registerSource + runtime (D-27)
- First live recordSignal→swap end-to-end on the seeded venue (REQ-06)
- reconcile.ts --assert: orphan == 0, exit 0 (D-40 — the phase acceptance gate)
- Agent hosted always-on + /healthz + UptimeRobot; RUN.md reproducibility + local failover (D-35/D-38/D-39/D-42); agent left running for the track record (D-36)
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-signals/01-06-SUMMARY.md` documenting the Phase 1
SourceRegistry address (the one Phase 5 cites), the minted agentId, the first live signal + swap tx hashes,
the reconciler gate result, the host + UptimeRobot setup, and the local-run failover drill outcome.
</output>
