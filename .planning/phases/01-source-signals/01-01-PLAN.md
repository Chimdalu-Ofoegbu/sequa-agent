---
phase: 01-source-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/v3-core
  - lib/v3-periphery
  - lib/openzeppelin-contracts-3.4
  - lib/solidity-lib
  - lib/base64-sol
  - foundry.toml
  - remappings.txt
  - src/mocks/MockERC20.sol
  - test/MockERC20.t.sol
  - script/DeployUniV3Fork.s.sol
  - script/DeployMocks.s.sol
  - script/SeedLiquidity.s.sol
  - script/VerifyPhase1.sh
  - src/config/SequaConstants.sol
  - deployments/sepolia.json
  - addresses.json
  - .env.example
autonomous: false
requirements: [REQ-01]
user_setup:
  - service: mantle-sepolia-rpc
    why: "Deploy fork + mocks + seed LP on chain 5003; needs a funded deployer + operator EOA with test MNT for gas"
    env_vars:
      - name: DEPLOYER_PRIVATE_KEY
        source: "Existing Phase 0 deployer EOA (already funded with Sepolia MNT)"
      - name: OPERATOR_PRIVATE_KEY
        source: "New agent operator EOA; fund with Sepolia MNT from the Mantle Sepolia faucet (holds trading capital + gas, D-26)"
      - name: MANTLESCAN_API_KEY
        source: "Existing — Mantlescan API key used by Phase 0 verify"

must_haves:
  truths:
    - "A canonical Uniswap V3 fork (factory + NonfungiblePositionManager + SwapRouter + QuoterV2) is live on Mantle Sepolia with codesize > 0 at every pinned address (D-43)"
    - "Four mock ERC-20s (MockUSDC=6dec, MockWMNT/MockMETH/MockWETH=18dec) are deployed and verified on Mantle Explorer (D-17/D-18/D-23)"
    - "Three full-range LP pools (WMNT/USDC, mETH/USDC, WETH/USDC) at fee=3000 are seeded at mainnet-like prices (D-19/D-20)"
    - "QuoterV2.quoteExactInputSingle returns a non-zero amountOut for each seeded pool"
    - "Venue + token + pool addresses are written back into SequaConstants.sol and addresses.json as deploy-time output, NOT copied from stale FusionX docs (D-21/D-43)"
  artifacts:
    - path: "src/mocks/MockERC20.sol"
      provides: "Parameterized-decimals mock ERC-20 with public mint"
      contains: "function decimals() public view override returns (uint8)"
    - path: "script/DeployUniV3Fork.s.sol"
      provides: "UniV3 periphery deploy script (D-43)"
    - path: "script/SeedLiquidity.s.sol"
      provides: "Pool create + full-range LP mint at target prices"
    - path: "addresses.json"
      provides: "Deploy-time venue/token/pool addresses for the TS runtime"
      contains: "swapRouter"
  key_links:
    - from: "script/SeedLiquidity.s.sol"
      to: "NonfungiblePositionManager.createAndInitializePoolIfNecessary"
      via: "idempotent pool create+init then NPM.mint"
      pattern: "createAndInitializePoolIfNecessary"
    - from: "src/config/SequaConstants.sol"
      to: "deployed UniV3 fork venue"
      via: "deploy-time write-back of venue addresses"
      pattern: "UNIV3_SWAP_ROUTER_SEPOLIA"
---

<objective>
Self-deploy the trading venue and seed liquidity that ALL downstream Phase 1 work depends on.
FusionX V3 is verifiably NOT on Mantle Sepolia (chain 5003) — every pinned address returns codesize 0
(RESEARCH Pitfall 1, DEC-001 amendment). Per D-43 (user-approved), deploy a canonical Uniswap V3 fork
(factory + NonfungiblePositionManager + SwapRouter + QuoterV2) alongside 4 mock ERC-20s, then seed 3
full-range LP pools at mainnet-like prices.

Purpose: This is the Wave-1 venue gate that blocks downstream LP/swap/quote work (frontmatter `wave: 1`). It produces the venue addresses, token addresses, and pool state
that the SourceRegistry redeploy (Plan 02 is independent), the chain hot path (Plan 04), the QuoterV2
price poll (Plan 05), and the reconciler (Plan 05) all consume. A canonical UniV3 factory enables
fee=3000 / tickSpacing 60 by default, so D-19's 0.30% tier is preserved unchanged.

Output: deployed + verified venue + 4 mocks + 3 seeded pools; venue/token/pool addresses written back
into SequaConstants.sol (Sepolia slots) and a new addresses.json for the TypeScript runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-signals/01-CONTEXT.md
@.planning/phases/01-source-signals/01-RESEARCH.md
@.planning/phases/01-source-signals/01-PATTERNS.md
@.planning/phases/01-source-signals/01-SPIKE-FINDINGS.md
@.planning/intel/decisions.md

<interfaces>
<!-- Existing repo patterns to copy. Executor uses these directly — no exploration needed. -->

Foundry deploy-script idiom (from script/DeployPhase0.s.sol — copy verbatim):
```solidity
import {Script, console2} from "forge-std/Script.sol";
contract DeployX is Script {
    function run() external {
        uint256 deployerKey = vm.envUint("DEPLOYER_PRIVATE_KEY");
        vm.startBroadcast(deployerKey);
        // new ...
        vm.stopBroadcast();
        console2.log("Name:", address(x));
    }
}
```
Run invocation: `forge script script/X.s.sol:X --rpc-url mantle_sepolia --broadcast --slow`

Toolchain (foundry.toml, current): solc 0.8.24, evm_version=paris, optimizer 200 runs, remapping `@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/` in remappings.txt.
⚠ Task 1 CHANGES this per **01-SPIKE-FINDINGS.md §c** (spike-proven): the hard `solc="0.8.24"` pin becomes `auto_detect_solc=true` (so 0.7.6 UniV3 + 0.8.x src coexist), remappings move INTO foundry.toml (root `remappings.txt` deleted, `auto_detect_remappings=false`), and a second OZ remapping `@openzeppelin-v3/contracts/=lib/openzeppelin-contracts-3.4/contracts/` is added. `src/` pragmas stay 0.8.24.

SequaConstants.sol Sepolia token slots to FILL (currently commented TODO[Phase 1] at lines 59-62):
```solidity
// address internal constant WMNT_SEPOLIA = 0x...; // PINNED IN PHASE 1
// address internal constant METH_SEPOLIA = 0x...; // PINNED IN PHASE 1
// address internal constant WETH_SEPOLIA = 0x...; // PINNED IN PHASE 1
// address internal constant USDC_SEPOLIA = 0x...; // PINNED IN PHASE 1
```
The dead FUSIONX_V3_*_SEPOLIA constants at lines 13-15 MUST be marked not-for-use (codesize 0 on 5003).

Etherscan V2 verify (from script/VerifyPhase0.sh — copy URL + flags verbatim, RESEARCH Pitfall 4):
```bash
VERIFIER_URL="https://api.etherscan.io/v2/api?chainid=5003"
forge verify-contract "${ADDR}" path:Contract --chain 5003 \
  --verifier etherscan --verifier-url "${VERIFIER_URL}" \
  --etherscan-api-key "${MANTLESCAN_API_KEY}" --watch
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor UniV3 periphery + write MockERC20 + tests</name>
  <read_first>
    - src/mocks/MockERC20.sol (target — does not exist yet)
    - src/SourceRegistry.sol lines 1-5 (SPDX + pragma + OZ import idiom to copy)
    - remappings.txt (existing @openzeppelin remapping)
    - .planning/phases/01-source-signals/01-RESEARCH.md lines 453-463 (exact MockERC20 shape) + Don't-Hand-Roll table
    - .planning/phases/01-source-signals/01-PATTERNS.md "MockERC20" + "No Analog Found" (UniV3 periphery vendoring)
    - .planning/phases/01-source-signals/01-SPIKE-FINDINGS.md § Spike #2 (AUTHORITATIVE, spike-proven vendoring recipe — overrides any stale instruction below)
  </read_first>
  <action>
    Vendor the canonical UniV3 periphery into lib/ (no in-repo analog — PATTERNS "No Analog Found").
    **FOLLOW 01-SPIKE-FINDINGS.md § Spike #2 EXACTLY — that recipe is spike-proven (COMPILES-CLEAN, deploy+mint
    green). It supersedes the pre-spike assumptions that were here.** Summary of the proven steps (a-d for Task 1):
    (a) Install: `forge install Uniswap/v3-core` (v1.0.0), `forge install Uniswap/v3-periphery` (v1.3.0),
        `forge install openzeppelin-contracts-3.4=OpenZeppelin/openzeppelin-contracts@v3.4.1-solc-0.7-2`,
        `forge install Uniswap/solidity-lib`, `forge install base64-sol=Brechtpd/base64@v1.1.0`.
        NOTE: Foundry 1.5.1 REMOVED `--no-commit` (no-commit is default; passing the flag errors).
    (b) Rewrite v3-periphery's OZ imports to a dedicated prefix (v3-core needs no change):
        `grep -rl "@openzeppelin/contracts/" lib/v3-periphery/contracts --include=*.sol | xargs sed -i 's#@openzeppelin/contracts/#@openzeppelin-v3/contracts/#g'`
        ⚠ Do NOT use Foundry contextual remappings (`context:prefix=target`) — the spike proved they SILENTLY
        FAIL on Windows Foundry 1.5.1 (imports fall back to OZ v5 and fail to compile). The prefix-rewrite is the fix.
    (c) Move remappings into foundry.toml, DELETE root remappings.txt, set `auto_detect_solc=true` (replaces the
        `solc="0.8.24"` pin so 0.7.6 + 0.8.x coexist) and `auto_detect_remappings=false`. Add BOTH OZ remappings:
        `@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/` (src→OZ v5) AND
        `@openzeppelin-v3/contracts/=lib/openzeppelin-contracts-3.4/contracts/` (periphery→OZ 3.4), plus
        `@uniswap/v3-core/=lib/v3-core/`, `@uniswap/v3-periphery/=lib/v3-periphery/`, `@uniswap/lib/=lib/solidity-lib/`,
        `base64-sol/=lib/base64-sol/`. (Exact block in SPIKE-FINDINGS §c.)
    (d) **PATCH `POOL_INIT_CODE_HASH`** in `lib/v3-periphery/contracts/libraries/PoolAddress.sol`. The spike PROVED
        the baked-in mainnet constant (`0xe34f199b…b8b54`) does NOT match this repo's locally-compiled pool bytecode
        and deploy/mint REVERTS without the patch. For repo settings (200 runs / paris / ipfs) the computed hash is
        `0x3c168cc5d3311f0933f08b32142d0998baeecd13571089b4bb0cdeeaf401d70b` — set the constant to it. Write a tiny
        0.8 test that logs `keccak256(type(UniswapV3Pool).creationCode)` via `vm.getCode` and ASSERTS it equals the
        patched constant (re-run after any optimizer/evm_version change — the hash shifts). This replaces the old
        false "the constant matches" assumption.

    Create src/mocks/MockERC20.sol (SPDX MIT, pragma ^0.8.24 to match the repo/OZ v5.0.2 toolchain — copy header from SourceRegistry.sol):
    ```solidity
    import {ERC20} from "@openzeppelin/contracts/token/ERC20/ERC20.sol";
    contract MockERC20 is ERC20 {
        uint8 private immutable _dec;
        constructor(string memory n, string memory s, uint8 d) ERC20(n, s) { _dec = d; }
        function decimals() public view override returns (uint8) { return _dec; }
        function mint(address to, uint256 amount) external { _mint(to, amount); } // public per D-17
    }
    ```
    Public `mint` is intentionally ungated (D-17, testnet-only — see threat model T-1-02).

    Create test/MockERC20.t.sol: deploy MockERC20("USD Coin","mUSDC",6) and ("Wrapped MNT","mWMNT",18);
    assert decimals() returns 6 and 18 respectively (D-18); assert mint(addr, 1_000e6) increases
    balanceOf(addr) by 1_000e6 and totalSupply by 1_000e6.
  </action>
  <verify>
    <automated>forge build && forge test --match-contract MockERC20Test -vvv</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` compiles BOTH the vendored canonical v3-core/v3-periphery @0.7.6 (OZ 3.4 via `@openzeppelin-v3/`) AND the repo's 0.8.x src/ contracts (OZ v5) in one project — no solc-version or OZ-version conflict (SPIKE-FINDINGS §a-c)
    - `forge test` keeps the EXISTING SourceRegistry/FollowRegistry suites green (coexistence — the vendoring must not break Phase 0 contracts)
    - `POOL_INIT_CODE_HASH` in `lib/v3-periphery/contracts/libraries/PoolAddress.sol` is PATCHED to the locally-computed value and a test asserts `keccak256(type(UniswapV3Pool).creationCode)` equals it (SPIKE-FINDINGS §d — the baked-in mainnet constant does NOT match and would revert deploy/mint). The Task-2 seed-time slot0 round-trip price assert remains an additional guard.
    - `forge test --match-contract MockERC20Test` exits 0
    - `grep -n "createAndInitializePoolIfNecessary" lib/v3-periphery/contracts/interfaces/INonfungiblePositionManager.sol` returns a match (periphery present)
    - test asserts `MockERC20("USD Coin","mUSDC",6).decimals() == 6` and an 18-decimals mock returns 18
    - `grep -n "function mint(address to, uint256 amount) external" src/mocks/MockERC20.sol` matches (public mint, D-17)
  </acceptance_criteria>
  <done>UniV3 periphery vendored + remapped; MockERC20 compiles; MockERC20Test green proving 6/18 decimals and public mint.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy UniV3 fork + 4 mocks + seed 3 full-range pools, write back addresses</name>
  <read_first>
    - script/DeployPhase0.s.sol (deploy-script env-read + broadcast shell to copy)
    - script/VerifyPhase0.sh (Etherscan V2 verify pattern to mirror in VerifyPhase1.sh)
    - foundry.toml (rpc_endpoints.mantle_sepolia, [etherscan] block)
    - src/config/SequaConstants.sol lines 12-15, 54-62 (dead FusionX Sepolia consts + TODO token slots)
    - deployments/sepolia.json (manifest shape: contracts.<name>.{address,deployTx,explorerUrl,verificationUrl,verified})
    - .planning/phases/01-source-signals/01-RESEARCH.md Pattern 1 (lines 238-260) + Pitfall 3 (sqrtPriceX96 decimal gap)
    - .planning/phases/01-source-signals/01-PATTERNS.md "SeedLiquidity.s.sol" + "DeployUniV3Fork.s.sol"
  </read_first>
  <action>
    Create script/DeployUniV3Fork.s.sol (D-43): deploy UniswapV3Factory, then NonfungiblePositionManager,
    SwapRouter, and QuoterV2 constructed against that factory + a WETH9 (use MockWMNT address as the
    WETH9 the periphery wraps, since the venue is internal). Use the DeployPhase0 env-read + broadcast shell.
    ⚠ The script is solc 0.8 (forge-std `Script.sol` is >=0.8.13) and CANNOT `new UniswapV3Factory()` across the
    0.7.6 boundary — instantiate the venue via `deployCode("UniswapV3Factory.sol:UniswapV3Factory")`,
    `deployCode("NonfungiblePositionManager.sol:NonfungiblePositionManager", abi.encode(factory, weth9, address(0)))`
    (descriptor = `address(0)` — not needed for mint), `deployCode("SwapRouter.sol:SwapRouter", abi.encode(factory, weth9))`,
    `deployCode("QuoterV2.sol:QuoterV2", abi.encode(factory, weth9))`, and drive them through locally-declared
    interfaces (SPIKE-FINDINGS §e). Pre-req: `PoolAddress.sol` POOL_INIT_CODE_HASH already patched in Task 1.
    `console2.log` all 4 venue addresses. After deploy, assert `factory.feeAmountTickSpacing(3000) == 60`
    (canonical UniV3 enables fee=3000 / tickSpacing 60 by default — preserves D-19); if it returns 0, call
    `factory.enableFeeAmount(3000, 60)` from the factory owner before seeding.

    Create script/DeployMocks.s.sol (D-11/D-17/D-18): deploy MockERC20("USD Coin","mUSDC",6),
    ("Wrapped MNT","mWMNT",18), ("Mantle ETH","mMETH",18), ("Wrapped ETH","mWETH",18); then
    `mUSDC.mint(operator, 10_000e6)` where operator = `vm.addr(vm.envUint("OPERATOR_PRIVATE_KEY"))` (D-11,
    10,000 mUSDC starting balance). Also mint generous balances of all 4 tokens to the deployer for LP seeding.
    `console2.log` all 4 token addresses.

    Create script/SeedLiquidity.s.sol (D-19/D-20/D-21/D-22) following RESEARCH Pattern 1 exactly:
    for each of the 3 pairs (WMNT/USDC, mETH/USDC, WETH/USDC) at fee=3000:
      1. `INonfungiblePositionManager.createAndInitializePoolIfNecessary(token0, token1, 3000, sqrtPriceX96)`
         — idempotent (D-22). token0 = min(addr), token1 = max(addr) (ascending by address — Pitfall 3).
      2. `token0.approve(NPM, amount0); token1.approve(NPM, amount1);`
      3. `NPM.mint(MintParams{token0, token1, fee:3000, tickLower:-887220, tickUpper:+887220,
         amount0Desired, amount1Desired, amount0Min:0, amount1Min:0, recipient:deployer, deadline:block.timestamp+1200})`.
    Full-range ticks for tickSpacing 60 = -887220 / +887220 (RESEARCH lines 246-252). Compute sqrtPriceX96
    from D-20 reference prices (WMNT≈$0.60, mETH≈$3200, WETH≈$3200, USDC≈$1) accounting for the 6-vs-18
    decimal gap (USDC 6, others 18 → factor 10^12 — Pitfall 3). Use a tested encodePriceSqrt helper; do NOT
    hand-roll the sqrt. Seed ~5k USDC-equivalent depth each (D-19). After init, read `slot0().sqrtPriceX96`
    back and assert it round-trips to ≈ the target price before minting (Pitfall 3 guard).
    Add an admin re-depth path: a `topUp()` function that mints + adds more LP for <60s re-depth (D-22).

    Create script/VerifyPhase1.sh: mirror VerifyPhase0.sh; verify all 4 mocks (with `--constructor-args`
    encoding name/symbol/decimals per mock — the one addition over Phase 0's zero-arg verify) and the
    venue contracts. Use VERIFIER_URL `https://api.etherscan.io/v2/api?chainid=5003`.

    Write back ALL deployed addresses to: (a) src/config/SequaConstants.sol — fill the WMNT/METH/WETH/USDC
    _SEPOLIA token slots (lines 59-62) AND add new `UNIV3_*_SEPOLIA` venue constants (factory/router/quoterV2/NPM);
    mark the dead FUSIONX_V3_*_SEPOLIA consts (lines 13-15) with a `// DEAD on chain 5003 — DO NOT USE (D-43)`
    comment. (b) a new addresses.json at repo root for the TS runtime: `{ chainId:5003, venue:{factory,
    swapRouter,quoterV2,nonfungiblePositionManager}, tokens:{usdc,wmnt,meth,weth}, pools:{wmntUsdc,methUsdc,
    wethUsdc}, fee:3000, decimals:{usdc:6,wmnt:18,meth:18,weth:18} }`. (c) deployments/sepolia.json — add
    venue + mock + pool entries following the existing contracts.<name> shape.
    Add OPERATOR_PRIVATE_KEY + NOISE_BOT_PRIVATE_KEY to .env.example (names only — RESEARCH State Inventory).
  </action>
  <verify>
    <automated>forge build && bash -c 'set -a; source .env 2>/dev/null; set +a; for a in $(jq -r ".venue[], .tokens[], .pools[]" addresses.json); do cs=$(cast codesize "$a" --rpc-url https://rpc.sepolia.mantle.xyz); echo "$a codesize=$cs"; [ "$cs" -gt 0 ] || exit 1; done'</automated>
  </verify>
  <acceptance_criteria>
    - `cast codesize <addr> --rpc-url https://rpc.sepolia.mantle.xyz` > 0 for every venue address (factory/router/quoterV2/NPM), every mock token, and every pool in addresses.json (D-43 fail-closed liveness, T-1-06)
    - `cast call <factory> "feeAmountTickSpacing(uint24)(int24)" 3000 --rpc-url https://rpc.sepolia.mantle.xyz` returns 60 (fee=3000 enabled, D-19 preserved)
    - QuoterV2 read returns non-zero: `cast call <quoterV2> "quoteExactInputSingle((address,address,uint256,uint24,uint160))" ...` via simulate returns amountOut > 0 for WMNT→USDC
    - `grep -n "UNIV3_SWAP_ROUTER_SEPOLIA\|USDC_SEPOLIA" src/config/SequaConstants.sol` matches filled (not commented) constants
    - `grep -ni "DEAD on chain 5003" src/config/SequaConstants.sol` matches (dead FusionX consts flagged)
    - `jq -e '.venue.swapRouter and .tokens.usdc and .pools.wmntUsdc and .fee == 3000' addresses.json` exits 0
    - `cast call <usdc> "decimals()(uint8)" --rpc-url ...` returns 6; `cast call <usdc> "balanceOf(address)(uint256)" <operator>` returns 10000000000 (10,000 mUSDC, D-11)
  </acceptance_criteria>
  <done>UniV3 fork + 4 mocks + 3 seeded pools live on Sepolia, every address codesize>0, fee=3000/tickSpacing 60 confirmed, pools quote non-zero, addresses written back to SequaConstants.sol + addresses.json + sepolia.json, operator holds 10k mUSDC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify deployed venue + verified mocks + mainnet-like pool prices</name>
  <action>Checkpoint: after Tasks 1-2 deploy + verify the venue/mocks/pools, pause for the human to confirm verified source pages render and pool prices are mainnet-like before downstream plans trust these addresses. Steps in how-to-verify.</action>
  <what-built>Deployed a canonical UniV3 fork + 4 verified mock tokens + 3 seeded full-range LP pools on Mantle Sepolia (D-43, D-17..D-24). Ran VerifyPhase1.sh to submit verification for all mocks + venue.</what-built>
  <how-to-verify>
    1. Open each mock token on https://sepolia.mantlescan.xyz/address/&lt;addr&gt;#code (addresses in addresses.json `tokens`) — confirm "Contract Source Code Verified" renders (D-23).
    2. Open the SwapRouter + QuoterV2 venue addresses on the explorer — confirm code is present.
    3. Confirm the seed prices look mainnet-like: in a terminal run the QuoterV2 simulate for WMNT→USDC (1 WMNT in) and confirm ~0.60 mUSDC out, mETH→USDC (1 mETH in) ~3200 mUSDC out (D-20). If a price is off by ~10^12, the sqrtPriceX96 decimal-gap is wrong (Pitfall 3) — flag it.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 mocks show verified source and the 3 pools quote mainnet-like prices, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployer/operator EOA → Sepolia chain | Private keys sign deploy + seed txs; keys in gitignored .env |
| any caller → MockERC20.mint | Public mint is intentionally ungated (testnet-only) |
| stale docs → SequaConstants venue addresses | Dead FusionX Sepolia addresses must never be trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Spoofing/Elevation | DEPLOYER_PRIVATE_KEY / OPERATOR_PRIVATE_KEY leak | mitigate | Keys in gitignored .env; .env.example holds names only; testnet-only funds (D-41) cap blast radius; rotate on exposure |
| T-1-02 | Elevation | MockERC20 public `mint` | accept | Intentional per D-17 (testnet liquidity tool); mock tokens have zero real value; Sepolia-only (D-41) |
| T-1-06 | DoS | wrong-venue / no-code address (sends to dead FusionX const) | mitigate | `cast codesize` > 0 asserted for every pinned venue address before trust (Pitfall 1); dead FusionX consts flagged DO-NOT-USE; venue is deploy-time write-back, never copied from stale docs (D-43) |
| T-1-09 | Tampering | wrong sqrtPriceX96 (6-vs-18 decimal gap seeds pool at wrong price) | mitigate | Tested encodePriceSqrt helper (no hand-rolled sqrt); slot0 round-trip assert ≈ D-20 target before LP mint (Pitfall 3); human-verify checkpoint confirms mainnet-like quotes |
</threat_model>

<verification>
- `forge build` clean with vendored UniV3 periphery
- `forge test --match-contract MockERC20Test` green
- Every address in addresses.json has codesize > 0 on chain 5003
- factory.feeAmountTickSpacing(3000) == 60
- All 4 mocks verified on Mantle Explorer (human-verify checkpoint)
</verification>

<success_criteria>
- UniV3 fork (factory/NPM/SwapRouter/QuoterV2) + 4 mocks + 3 full-range fee=3000 pools live + verified on Sepolia
- Venue/token/pool addresses written back to SequaConstants.sol (Sepolia slots + UNIV3_* consts), addresses.json, deployments/sepolia.json
- Dead FusionX Sepolia consts flagged DO-NOT-USE
- Operator EOA holds 10,000 mUSDC; pools quote mainnet-like prices
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-signals/01-01-SUMMARY.md` documenting the deployed
venue/token/pool addresses (the single source of truth downstream plans read), the chosen UniV3 periphery
fork + its POOL_INIT_CODE_HASH handling, and any deviation from D-20 seed prices.
</output>
