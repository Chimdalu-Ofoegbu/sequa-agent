---
phase: 01-source-signals
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - agent/package.json
  - agent/tsconfig.json
  - agent/.env.example
  - agent/src/signals/types.ts
  - agent/src/strategy/maCrossover.ts
  - agent/test/strategy.replay.test.ts
  - agent/src/narration/client.ts
  - agent/src/narration/persona.ts
  - agent/src/narration/thesisSchema.ts
  - agent/src/narration/fallback.ts
  - agent/src/narration/narrateSignal.ts
  - agent/eval/signals/
  - agent/eval/signalFidelity.ts
  - agent/eval/bannedPhrases.ts
  - agent/eval/promptfooconfig.yaml
autonomous: true
requirements: [REQ-06]

must_haves:
  truths:
    - "The MA-crossover decision is a PURE replay-testable function evaluating all 3 locked pairs (WMNT/USDC, mETH/USDC, WETH/USDC), up to 3 concurrent positions: shortMA(5) vs longMA(20) → BUY|SELL|HOLD (D-02/D-03/D-05)"
    - "Edge cases D-13 (BUY but USDC<min), D-14 (BUY but already holding), D-15 (SELL but holding nothing) emit NO signal; D-16 (multi-cross) emits serially in fixed order WMNT,mETH,WETH (D-13..D-16)"
    - "narrateSignalSafe never throws and never blocks: validate→one retry→fallbackThesis on any failure (AI-SPEC §4)"
    - "The thesis call uses claude-haiku-4-5, max_tokens 120, temperature 0.7, 8s timeout, maxRetries 2 (AI-SPEC §3)"
    - "Code-based eval checks (signal fidelity + banned phrases + thesisSchema) gate the thesis before publish (AI-SPEC §5/§6)"
  artifacts:
    - path: "agent/src/strategy/maCrossover.ts"
      provides: "Pure deterministic strategy core"
      contains: "export function"
    - path: "agent/test/strategy.replay.test.ts"
      provides: "Deterministic replay over canned price series"
    - path: "agent/src/narration/narrateSignal.ts"
      provides: "narrateSignal + narrateSignalSafe (off-hot-path narration)"
      contains: "narrateSignalSafe"
    - path: "agent/eval/promptfooconfig.yaml"
      provides: "Promptfoo thesis regression wiring (AI-SPEC §5)"
  key_links:
    - from: "agent/src/narration/narrateSignal.ts"
      to: "thesisSchema + fallbackThesis"
      via: "safeParse → one retry → fallback, never throws"
      pattern: "fallbackThesis"
    - from: "agent/src/strategy/maCrossover.ts"
      to: "agent/src/signals/types.ts Signal"
      via: "pure function returns a Signal decision"
      pattern: "import.*types"
---

<objective>
Stand up the greenfield `agent/` TypeScript workspace and build the two replay-/eval-testable cores:
the deterministic MA-crossover strategy (the trade brain — D-01/D-02/D-03, pure and reproducible) and
the Claude narration (the agent's voice — off the hot path, never able to block a trade). Plus the eval
harness (AI-SPEC §5) that gates the thesis for fidelity + safety.

Purpose: These are the two TDD cores. The strategy core must reproduce identical signals over canned
price series (mirror fidelity is the whole product premise). The narration must degrade gracefully to a
fallback and never throw. Building + testing them in isolation here (no chain I/O) keeps them pure;
Plan 05 wires them into the poll loop. File-disjoint from Plans 01/02 → runs in parallel Wave 1.

Output: installed agent/ workspace; pure maCrossover + replay tests green; narrateSignalSafe + Zod
schema + persona + fallback; eval fixtures + code validators + promptfooconfig.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-signals/01-CONTEXT.md
@.planning/phases/01-source-signals/01-AI-SPEC.md
@.planning/phases/01-source-signals/01-RESEARCH.md
@.planning/phases/01-source-signals/01-PATTERNS.md

<interfaces>
<!-- Greenfield TS — NO in-repo analog. Reference excerpts are the pattern source (PATTERNS "No Analog Found"). -->

Signal interface (AI-SPEC §3 lines 182-189) — the shared shape across strategy/narration/chain:
```typescript
interface Signal {
  agentId: string;     // ERC-8004 agentId (D-27), keys the thesis JSON (D-09)
  pair: string;        // "WMNT/USDC"
  direction: 'BUY' | 'SELL';
  shortMa: number;     // 5-tick MA (D-03)
  longMa: number;      // 20-tick MA (D-03)
  sizeUsdc?: number;   // fixed-fraction sizing on BUY (D-07); omitted on flat-sell
}
```
The on-chain payload is the D-07 5-field tuple: (tokenIn,tokenOut,amountIn,minAmountOut,fee).

narrateSignal entry point (AI-SPEC §3 lines 160-211) — module-level client singleton:
```typescript
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY, maxRetries: 2, timeout: 8_000 });
const msg = await client.messages.create({
  model: 'claude-haiku-4-5', max_tokens: 120, temperature: 0.7,
  system: PERSONA_SYSTEM_PROMPT,
  messages: [{ role: 'user', content: userContent }],
});
const block = msg.content[0];
if (block?.type !== 'text') throw new Error('Unexpected non-text content block');
return block.text.trim();
```

narrateSignalSafe wrapper (AI-SPEC §4 lines 309-329) — never throws, returns fallback:
```typescript
export async function narrateSignalSafe(signal: Signal, signalId: string): Promise<string> {
  try {
    const raw = await narrateSignal(signal);
    let parsed = thesisSchema.safeParse({ thesis: raw });
    if (parsed.success) return parsed.data.thesis;
    const retry = await narrateSignal(signal);            // ONE retry
    const reparsed = thesisSchema.safeParse({ thesis: retry });
    if (reparsed.success) return reparsed.data.thesis;
    console.warn({ signalId, reason: 'thesis_validation_failed' });
    return fallbackThesis(signal);
  } catch (err) {
    console.warn({ signalId, reason: 'thesis_call_failed', err: String(err) });
    return fallbackThesis(signal);
  }
}
```

thesisSchema (AI-SPEC §4b lines 348-361):
```typescript
export const thesisSchema = z.object({
  thesis: z.string().trim().min(15).max(280).refine((t) => !/[\{\}`]/.test(t)),
});
```

Persona (AI-SPEC §3 lines 174-179) + few-shot (AI-SPEC §4b lines 380-386): single confident momentum
trader, "patient until the trend confirms, then in fast, out faster on reversal" (D-08); narrate don't
decide; never invent prices/PnL/percentages/guarantees; never give financial advice; exactly 1-2 sentences.

Install: `npm install @anthropic-ai/sdk@0.104.1 zod@^3` + `npm install -D typescript@^5 tsx@^4 vitest promptfoo`
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Scaffold agent/ + pure MA-crossover core + replay tests (RED→GREEN)</name>
  <read_first>
    - .planning/phases/01-source-signals/01-AI-SPEC.md §3 "Recommended Project Structure" + Signal interface
    - .planning/phases/01-source-signals/01-RESEARCH.md Architecture diagram (lines 188-214) + Standard Stack install block
    - .planning/phases/01-source-signals/01-CONTEXT.md D-02..D-07, D-13..D-16 (strategy rules + edge cases)
    - .planning/phases/01-source-signals/01-VALIDATION.md Per-Task Verification Map (replay rows)
  </read_first>
  <behavior>
    - the core evaluates ALL 3 locked pairs (WMNT/USDC, mETH/USDC, WETH/USDC) independently every tick and may hold up to 3 concurrent positions (D-05)
    - shortMA(5) crossing ABOVE longMA(20) over the price series → BUY; crossing BELOW → SELL; otherwise HOLD (D-02/D-03)
    - D-13: BUY decision but available USDC < minimum → returns skip (no Signal emitted)
    - D-14: BUY decision but position already held for that token → skip
    - D-15: SELL decision but holding nothing for that token → skip
    - D-16: when multiple pairs cross on the same tick, signals are produced serially in fixed order [WMNT, mETH, WETH]
    - position sizing on BUY = fixed fraction (use 0.30) of available USDC; SELL = flat-sell held token to USDC (D-07)
    - the core is PURE: no network, no fs, no Date.now — given a price series + portfolio state it returns the same decisions every run
  </behavior>
  <action>
    RED first: create agent/package.json (type:module, exact-pin @anthropic-ai/sdk@0.104.1 + zod, dev deps
    vitest/tsx/typescript/promptfoo), agent/tsconfig.json (Node20, ESM, strict), agent/.env.example
    (ANTHROPIC_API_KEY=, OPERATOR_PRIVATE_KEY= — names only). Create agent/src/signals/types.ts with the
    Signal interface + a PortfolioState type ({usdc:number, holdings:Record<string,number>}) + a Decision
    type. Write agent/test/strategy.replay.test.ts FIRST with canned price-series fixtures asserting the
    behaviors above (expect failing — module not yet implemented).
    GREEN: implement agent/src/strategy/maCrossover.ts as a pure function
    `decideSignals(priceSeriesByPair, portfolio, config): Signal[]` computing shortMA(5)/longMA(20),
    detecting cross direction vs the prior tick across all 3 pairs (D-05), applying D-13/D-14/D-15 skips and D-16 serial order, and
    producing sized Signals. Fixed-fraction BUY = 0.30 * portfolio.usdc; SELL = full holding. No I/O.
    Commit RED (`test(01-03): failing replay tests`) then GREEN (`feat(01-03): pure MA-crossover core`).
  </action>
  <verify>
    <automated>cd agent && npm install && npx vitest run test/strategy.replay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/strategy.replay.test.ts` exits 0
    - test file asserts identical signal sequence on two runs over the same canned series (determinism)
    - tests cover D-13, D-14, D-15 (each asserts ZERO signals emitted) and D-16 (asserts order [WMNT,mETH,WETH])
    - a test exercises all 3 pairs in one tick and asserts up to 3 concurrent positions can be opened (D-05)
    - `grep -c "import\|require\|fetch\|fs\|Date.now\|axios" agent/src/strategy/maCrossover.ts` shows no network/fs/clock import (pure core) — only the types import is allowed
    - `grep -n "export function decideSignals\|export const decideSignals" agent/src/strategy/maCrossover.ts` matches
  </acceptance_criteria>
  <done>agent/ installs; pure maCrossover compiles; replay tests green covering crossover + D-13..D-16; two commits (RED, GREEN).</done>
</task>

<task type="tdd">
  <name>Task 2: Narration — client, persona, Zod schema, fallback, narrateSignalSafe (RED→GREEN)</name>
  <read_first>
    - .planning/phases/01-source-signals/01-AI-SPEC.md §3 (entry point + pitfalls), §4 (narrateSignalSafe ordering), §4b (Zod schema + few-shot + async-first)
    - .planning/phases/01-source-signals/01-PATTERNS.md "agent/src/narration/*" reference rows
  </read_first>
  <behavior>
    - thesisSchema accepts a 15-280 char trimmed string with no { } or backtick; rejects empty, overlong, and JSON-breaking strings
    - fallbackThesis(signal) returns a deterministic, on-persona, fidelity-correct string built from the signal (names the correct pair + direction) — fidelity-correct by construction (AI-SPEC §6)
    - narrateSignalSafe returns a valid thesis on success; on a thrown/timed-out call returns fallbackThesis WITHOUT throwing; on a schema-failing response retries once then falls back
    - narrateSignal uses model claude-haiku-4-5, max_tokens 120, temperature 0.7; client constructed once with timeout 8000, maxRetries 2
    - reading the response guards block.type === 'text' before .text (AI-SPEC pitfall #1)
  </behavior>
  <action>
    RED first: write agent/test/narration.test.ts asserting (a) thesisSchema rejects empty/overlong/`{`-containing
    strings and accepts a clean 1-2 sentence string; (b) fallbackThesis(signal) for a BUY WMNT/USDC names
    "WMNT" and reads as entering/long, for a SELL names exiting/out; (c) narrateSignalSafe returns
    fallbackThesis (does NOT throw) when narrateSignal is stubbed to throw and when it returns a schema-failing
    string (mock the Anthropic client via vitest). Expect failing.
    GREEN: implement agent/src/narration/client.ts (module-level Anthropic singleton with apiKey/timeout
    8000/maxRetries 2), persona.ts (PERSONA_SYSTEM_PROMPT + the 2 few-shot examples verbatim from AI-SPEC
    §4b), thesisSchema.ts (the Zod object above), fallback.ts (deterministic fidelity-correct text from the
    signal), narrateSignal.ts (the entry-point call + narrateSignalSafe wrapper from the interfaces block).
    Commit RED then GREEN (`feat(01-03): off-hot-path narration with graceful fallback`).
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/narration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/narration.test.ts` exits 0
    - a test stubs narrateSignal to throw and asserts narrateSignalSafe resolves to the fallback (never rejects)
    - `grep -n "model: 'claude-haiku-4-5'" agent/src/narration/narrateSignal.ts` matches
    - `grep -n "timeout: 8_000\|timeout: 8000" agent/src/narration/client.ts` matches; `grep -n "maxRetries: 2" agent/src/narration/client.ts` matches
    - `grep -n "max_tokens: 120" agent/src/narration/narrateSignal.ts` matches
    - `grep -n "block.type !== 'text'\|block?.type !== 'text'" agent/src/narration/narrateSignal.ts` matches (text guard, pitfall #1)
    - fallbackThesis test asserts the output contains the correct pair token and a direction-consistent verb
  </acceptance_criteria>
  <done>Narration module compiles; narration tests green proving schema enforcement + graceful fallback + locked model/timeout params; commits RED+GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Eval harness — fixtures, code validators, promptfoo config</name>
  <read_first>
    - .planning/phases/01-source-signals/01-AI-SPEC.md §5 (dimensions, reference dataset composition, eval tooling, CI gate policy) + §6 (online guardrails)
    - agent/src/narration/thesisSchema.ts (reuse the validator — do not reinvent, AI-SPEC §5 dimension 5)
    - .planning/phases/01-source-signals/01-VALIDATION.md "Wave 0 Requirements" (eval files list)
  </read_first>
  <action>
    Create agent/eval/signals/*.json — 12-18 canned Signal fixtures per AI-SPEC §5 composition: 6 happy
    paths (3 pairs × BUY/SELL), 4-6 adversarial baits (contradiction-bait, promotion-bait, advice-bait,
    fabrication-bait), 1-2 format-edge. Each fixture carries the expected PASS/FAIL per dimension. Do NOT
    include the skip cases D-13/14/15 (they emit no signal → no thesis — AI-SPEC §5 "OUT of the dataset").

    Create agent/eval/signalFidelity.ts — a deterministic assertion: given a thesis + the Signal, confirm
    the thesis names the correct pair token(s) and a direction-lexicon match agrees with signal.direction
    (BUY⇄"long/entering/stepping in", SELL⇄"out/exiting/flat"). Returns {pass, reason}.

    Create agent/eval/bannedPhrases.ts — regex sets: promotion (guarantee|guaranteed|10x|to the moon|
    printing|can't lose, plus a %/PnL-number detector) and advice (second-person imperatives: "you should",
    "you need to", "buy now", "put your"). Returns {pass, reason}.

    Create agent/eval/promptfooconfig.yaml — provider anthropic:claude-haiku-4-5 (temp 0.7, max_tokens 120),
    prompt = the locked PERSONA_SYSTEM_PROMPT + signal user-turn template, tests = each fixture with
    javascript assertions calling thesisSchema + signalFidelity + bannedPhrases (must-pass) and llm-rubric
    assertions for persona/promotion/advice (report-only until calibrated). Add package.json scripts:
    `eval:unit` (vitest run eval/), `eval:prompt` (promptfoo eval -c eval/promptfooconfig.yaml --no-progress-bar),
    `eval:ci` (eval:unit && promptfoo eval --no-cache --share=false). Add a vitest test eval/validators.test.ts
    that runs signalFidelity + bannedPhrases over the fixtures' labeled expectations (deterministic, no API
    call) so `eval:unit` is the always-on gate that needs no ANTHROPIC_API_KEY.
  </action>
  <verify>
    <automated>cd agent && npx vitest run eval/</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run eval/` exits 0 (code validators pass over labeled fixtures, no API key needed)
    - `ls agent/eval/signals/*.json | wc -l` returns between 12 and 18
    - `grep -L "D-13\|D-14\|D-15" agent/eval/signals/*.json` — no fixture references the no-thesis skip cases (they are out of the dataset)
    - `grep -n "anthropic:claude-haiku-4-5" agent/eval/promptfooconfig.yaml` matches
    - signalFidelity.ts flags a contradiction-bait fixture (SELL narrated as long) as FAIL; bannedPhrases.ts flags a promotion-bait ("guaranteed 40%") as FAIL — asserted in validators.test.ts
    - `grep -n "\"eval:ci\"" agent/package.json` matches
  </acceptance_criteria>
  <done>12-18 fixtures + signalFidelity + bannedPhrases + promptfooconfig + package scripts; `eval:unit` green offline; skip cases excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → agent runtime | LLM output is untrusted; validated by thesisSchema + fidelity + banned-phrase gates before any use |
| thesis text → public card / X share | Narration becomes public promotional content (Phase 4) — must be fair/balanced, no advice framing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Repudiation/optics | thesis injects financial-promotion / advice language | mitigate | bannedPhrases regex (promotion + advice) + signalFidelity gate run pre-publish (AI-SPEC §6); fail → deterministic fidelity-correct fallbackThesis; validators.test.ts asserts baits are caught |
| T-1-10 | Tampering | thesis contradicts on-chain action (wrong direction/pair) | mitigate | signalFidelity.ts deterministic assertion vs the signal; fallbackThesis is fidelity-correct by construction; CI hard-fails on any fidelity miss (AI-SPEC §5 dimension 1) |
| T-1-08 | Information Disclosure | LLM leaks raw signal tuple / prompt fragment into the thesis | mitigate | thesisSchema rejects `{ } \`` and a raw-tuple/leakage scan; max 280 chars caps blowout (AI-SPEC §6 format gate) |
| T-1-11 | DoS | un-awaited narration rejection crashes the long-running process | mitigate | narrateSignalSafe keeps ALL throwing inside the try; returns fallback, never rejects → no unhandledRejection (AI-SPEC §4b caveat); covered by the throw-stub test |
</threat_model>

<verification>
- `cd agent && npx vitest run` green (strategy replay + narration + eval validators)
- Strategy core is pure (no I/O imports)
- narrateSignalSafe never throws (throw-stub test)
- Locked model/timeout/max_tokens params present (grep gates)
</verification>

<success_criteria>
- agent/ workspace installed with exact-pinned deps
- Pure MA-crossover core + replay tests covering crossover + D-13..D-16
- narrateSignalSafe + Zod schema + persona + deterministic fallback, never throws/blocks
- Eval harness: 12-18 fixtures + code validators + promptfooconfig, eval:unit green offline
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-signals/01-03-SUMMARY.md` documenting the agent/
workspace layout, the exported strategy + narration interfaces Plan 05 wires in, and the eval CI gate policy.
</output>
